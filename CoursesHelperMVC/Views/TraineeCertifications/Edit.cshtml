@model CoursesHelperWebAPI.Models.App.TraineeCertification

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TraineeCertification</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" asp-route-traineeId="@Model.TraineeId" asp-route-certificationId="@Model.CertificationId">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TraineeId" />
            <input type="hidden" asp-for="CertificationId" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
